const toDos = [
    'Clean the house',
    'Walk the dog',
    'Work out',
    'Program',
    'Write a blog post'
]

console.log(
    `You have ${toDos.length} todos`
)

console.log(
    `To do: ${toDos[0]}`
)

console.log(
    `To do: ${toDos[toDos.length - 2]}`
)